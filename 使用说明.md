# WorkBuddy 使用说明

这个技能将道具图片处理为可放入生物岛建造游戏的 3D 道具：去背景 → 单道具保留整体或多物件分割 → 混元 3D → GLB、透明图标及元数据 → Unity 预制体、道具配置与工具栏注册。

本包只包含通用技能、脚本、使用说明和测试，不含示例道具图片或已生成模型。

## 1. 安装到 WorkBuddy

解压后，把整个 `image-parts-to-3d` 文件夹放到：

```text
%USERPROFILE%\.workbuddy\skills\image-parts-to-3d\
```

你这台电脑对应：

```text
C:\Users\ASUS\.workbuddy\skills\image-parts-to-3d\
```

检查目录应直接包含 `SKILL.md`、`使用说明.md`、`scripts`、`references`、`assets` 和 `tests`。不要多套一层同名文件夹。

如果已经安装旧版，先把旧文件夹移到技能目录以外作为备份，再放入新版。随后重新打开 WorkBuddy 或新建会话，让它重新发现技能。也可以在对话中明确指定实际 `SKILL.md` 文件路径，避免误用旧版本。

如果 WorkBuddy 提供本地技能导入入口，也可使用该入口安装，安装后确认上述文件完整即可。本包不是 Unity 的 `.unitypackage`，不要把整个压缩包拖进 Unity Assets。

## 2. 在 WorkBuddy 中调用

上传一张单个道具的裁剪图片，直接说：

> 调用 image-parts-to-3d 技能，把这张图片作为一个完整道具生成低面数 GLB，并导入 F:/unity/My project 的生物岛建造游戏。工具栏图标用本次去背后的透明原图，拖拽放置用真正的 3D 模型。先检查去背效果，再生成；使用 --no-split 和 --unity-copy。请通过 WorkBuddy 的 connect_cloud_service 获取临时凭证，不需要我提供 token。

如果要指定名字和分类，可补一句：

> 道具名称叫“木长凳”，稳定文件名用 wooden_bench，放到 Decoration 装饰分类，目标面数 20000。

如果只想生成文件，不立即写入游戏：

> 调用 image-parts-to-3d，把图片生成一个完整 GLB，并保留透明图标和元数据，先不要复制到 Unity。

如果一张图片里有多个独立物件：

> 调用 image-parts-to-3d，按独立物件分割后分别生成模型，每个模型对应自己的透明去背图。先告诉我将生成几个任务。

分类通常选 Building（建筑）、Plant（植物）、Decoration（装饰）。当前生物岛的 Terrain 分页用于地形编辑，因此石头等普通道具默认放 Decoration；Animal 没有普通道具分页。

## 3. 授权和运行环境

WorkBuddy 内部应先调用 `connect_cloud_service` 获取本次临时凭证，优先使用 `tempToken`，没有时才使用 `token`。新版支持 `--token-stdin`，由 WorkBuddy 把凭证传给脚本。**不需要把凭证粘贴给聊天，也不要把凭证写进本包。**

此授权由 WorkBuddy 提供，Python 脚本本身不会自动登录。若连接工具不可用、账户未登录或服务额度不足，需要先解决 WorkBuddy 报出的具体问题；这不是重新安装 Python 就能解决的。

Python 需要 `numpy` 和 `Pillow`。让 WorkBuddy 使用它实际可用的 Python，并把依赖装到同一个解释器。本机已存在的托管 Python 位于：

```text
C:\Users\ASUS\.workbuddy\binaries\python\versions\3.13.12\python.exe
```

其他电脑先确认实际解释器位置。混元客户端通常由技能自动在 WorkBuddy 内置插件缓存中找到；只有找不到时才用 `BUDDY_CLOUD_SCRIPT` 指向真实的 `buddy-cloud.py`。

生成会使用实际服务额度：一个整体道具提交一个任务，多物件按件提交。收费、额度和时间以 WorkBuddy 当前返回的信息为准。`--dry-run` 和已有模型的 Unity 复制重试不会提交 3D 生成任务。

## 4. Unity 自动导入会做什么

默认工程：`F:/unity/My project`。默认场景：`Assets/DropBY.unity`。

| 内容 | 工程内位置 |
|---|---|
| GLB 模型 | `Assets/AnimalHome/Imported3D/SceneProps` |
| 原图图标 | `Assets/AnimalHome/Icons/SourceCutouts` |
| 道具预制体 | `Assets/AnimalHome/Prefabs/AH_<itemId>.prefab` |
| 道具配置 | `Assets/AnimalHome/Data/Items/<itemId>.asset` |

`--unity-copy` 会安装配套 Editor 桥接，备份并小范围更新旧导入器，复制模型、图标和同名 `.prop.json`。随后 Unity 创建/更新预制体和配置，并同步工具栏和放置目录。

首次安装或升级时，如果 Unity 正在运行，脚本可能先安装桥接并提示等待编译。让 Unity 编译完成后重跑同一命令，此准备阶段不会先提交收费生成。也可关闭 Unity 完成复制后再打开工程。

已有属性默认保留；只有明确指定 `--unity-update-metadata` 时才更新传入的设计字段。模型更新会重建受管理的模型子树，长期保留的自定义组件请放在预制体根节点或管理子树以外。

模型初始会居中、贴地并归一化，但生成后仍要检查方向、尺寸和占地。当前游戏格子较细，不要把默认占地当成精确几何包围范围。完整字段和规则见 [Unity 导入说明](references/unity-import.md)。

## 5. 判断是否真的完成

完成分为三步：

1. 本地已有 GLB、透明 PNG、`.prop.json` 和 `manifest.json`。
2. 文件已经复制进 Unity，等待 Editor 完成导入。
3. `Library/ImagePartsTo3D/<itemId>.status.json` 显示 `registered`，并在游戏对应分类中看到图标、能拖出 3D 模型。

只看到“复制成功”不能认定工具栏已经可用。可在 Unity 手动执行：

**Tools → Drop By → 图片转3D → 同步模型、原图图标和道具栏**。

如果报告 `pending_scene_save`，表示场景原本有未保存修改；正常保存场景后再检查。如果显示 `pending_visibility`，检查分类。其他错误见 [故障排查](references/troubleshooting.md)。

## 6. 失败后继续，不重复生成

如果模型已生成，只是复制或 Unity 导入失败，告诉 WorkBuddy：

> 不要重新生成模型。使用已有产物目录的 manifest.json，通过 scripts/unity_export.py --manifest 重试 Unity 导入，继续使用原来的去背 PNG。

对应命令示例（在实际技能目录执行，替换产物路径）：

```powershell
python scripts/unity_export.py --manifest "D:/3d-assets/wooden_bench/manifest.json" --unity-copy --unity-project "F:/unity/My project"
```

这一步不需要混元凭证。如果原任务只是超时，先按 manifest 中的 job_id 查询原任务，再考虑重新提交。

## 7. 可选的命令方式

一般直接与 WorkBuddy 对话即可。以下是给 WorkBuddy 执行的通用示例，`--token-stdin` 需要它同时供给临时凭证，单独手动粘贴这条命令会等待输入：

```powershell
python scripts/pipeline.py --input "D:/images/prop.png" --work-dir "D:/3d-assets" --name wooden_bench --no-split --generate-type LowPoly --face-count 20000 --display-name "木长凳" --category Decoration --unity-copy --unity-project "F:/unity/My project" --token-stdin
```

只检查去背，不用凭证：

```powershell
python scripts/pipeline.py --input "D:/images/prop.png" --work-dir "D:/3d-assets" --name wooden_bench --no-split --dry-run
```

所有参数见 `SKILL.md` 或 `python scripts/pipeline.py --help`。

## 验证范围

包内有离线自动测试；可在技能目录运行 `python -m unittest discover -s tests -v`。Unity 桥接已通过实际工程对应程序集的编译检查。真实云端生成、特定模型的材质与游戏内拖拽，需要实际调用后验收；包中不包含任何凭证或预先生成的 GLB。
